{
  "mcpServers": {
    "lean-lsp": {
      "command": "uvx",
      "args": ["lean-lsp-mcp"]
    },
    "gemini": {
      "command": "uv",
      "args": ["run", "--python", "3.10", "--with", "mcp", "--with", "google-genai", "--with", "python-dotenv", "python", "mcp_gemini/server.py"]
    }
  }
}
